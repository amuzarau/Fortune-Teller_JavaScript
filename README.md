# Fortune-Teller_JavaScript
Fortune Teller program in JavaScript. Randomly selecting a fortune from the available fortunes with the Math.random() method and the Math.floor() method to generate random numbers
